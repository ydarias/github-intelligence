# CLI Runner

The first application will be a CLI that is able to load the issues from a Github repository.

The CLI should be implemented using the [commander](https://www.npmjs.com/package/commander) library.

To load the issues the command to execute will be like `node index.js --repo <owner>/<repo> --token <token>`.
